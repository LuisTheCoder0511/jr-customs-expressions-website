let state = sessionStorage.getItem("state")
if (state === null) state = "customer-item"
console.log("State: " + state)

function mcnav_build(){
    switch(state){
        case "customer-item":

            break
    }
}